# Kubernetes Secrets (TEMPLATE - no real values)
apiVersion: v1
kind: Secret
metadata:
  name: fte-secrets
  namespace: customer-success-fte
type: Opaque
stringData:
  # Database
  POSTGRES_PASSWORD: "changeme"
  DATABASE_URL: "postgresql://fte_user:changeme@postgres-service:5432/fte_db"

  # Cerebras API (NOT OpenAI - using Cerebras free tier)
  CEREBRAS_API_KEY: "csk-your-cerebras-api-key-here"
  CEREBRAS_MODEL: "llama3.1-8b"

  # Gmail
  GMAIL_CREDENTIALS: |
    {
      "type": "service_account",
      "project_id": "your-project",
      "private_key_id": "...",
      "private_key": "-----BEGIN PRIVATE KEY-----\n..."
    }

  # Twilio
  TWILIO_ACCOUNT_SID: "ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
  TWILIO_AUTH_TOKEN: "your-auth-token-here"
  TWILIO_WHATSAPP_NUMBER: "whatsapp:+14155238886"
